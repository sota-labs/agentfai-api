export enum MessageStatus {
  PROCESSING = 'processing',
  DONE = 'done',
  CANCELLED = 'cancelled',
  DELETED = 'deleted',
}

export enum ThreadStatus {
  ACTIVE = 'active',
  HIDDEN = 'hidden',
  DELETED = 'deleted',
}
